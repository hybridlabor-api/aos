.venv
.venv/
__pycache__/
*.pyc
node_modules/
dist/
build/
.DS_Store
*.db
*.log
.env
.env.*
.npmrc

# Temporary generator & test scripts
update_readme*.py
rebuild_readme*.py
generate_skills_table*.py
test_sandbox*.js
patch_installer*.js

# Installer output artifact — generated from skills/workspace_agents/, not source
.agents/skills/

# graph-gate.mjs progress marker — per-run scratch state, never source
production_artifacts/.graph-gate-last-block

# /startcycle per-cycle hand-off artifacts — overwritten every run, never source.
# Public repo history shouldn't churn on disposable internal plan/review notes.
production_artifacts/*.md
